# This is the same as [general.import]
[general]
import = [ "catppuccin-mocha.toml" ]

[terminal.shell]
program = { program = "powershell", args = ["-nologo"] }

[window]
dimensions = { columns = 130, lines = 40 }
padding = { x = 3, y = 3 }

[font]
normal = { family = "Hack Nerd Font", style = "Regular" }
size = 11

[mouse]
hide_when_typing = true

